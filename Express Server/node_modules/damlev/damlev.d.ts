/**
 * Clears the cached arrays, freeing memory that would otherwise be kept
 * forever.
 */
export declare function uncache(): void;
/**
 * Returns the edit distance between the source and target strings.
 * @param  {String} source
 * @param  {Strign} target
 * @return {Number}
 */
export default function damlev(source: string, target: string): number;
